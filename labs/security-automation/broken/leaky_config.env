# BROKEN FIXTURE -- intentionally contains a planted (fake) credential so that
# secret_scan.sh has a real, reproducible hit to find. The values below are the
# canonical AWS *documentation* example credentials (not a live key). Never
# commit a real one. The troubleshooting exercise expects this file to be FOUND.
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
aws_secret_access_key=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
DB_PASSWORD="hunter2-not-a-real-password"
