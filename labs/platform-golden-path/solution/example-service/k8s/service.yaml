apiVersion: v1
kind: Service
metadata:
  name: example-service
  labels:
    app.kubernetes.io/name: example-service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: example-service
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
