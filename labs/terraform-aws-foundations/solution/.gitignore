# Terraform working files — never commit these.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Real variable values and the live backend config stay local.
terraform.tfvars
*.auto.tfvars
backend.tf
override.tf
override.tf.json
*_override.tf
*_override.tf.json
